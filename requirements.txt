# Core GUI + plotting + serial
PyQt5>=5.15.9,<5.16
pyqtgraph>=0.13.4,<0.14
numpy>=2.1
pyserial>=3.5,<4.0
scipy>=1.13,<2.0

# PicoScope SDK wrapper (ps4000 driver family; install PicoSDK drivers separately)
picosdk-python-wrappers>=1.1

# Analysis stack for scalable histograms
polars>=1.6
# pyarrow>=15   # optional: enable IPC/Parquet exports if needed
# duckdb>=0.9   # optional: in-memory analytics for archival data
