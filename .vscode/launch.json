{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Instrument App",
            "type": "python",
            "request": "launch",
            "module": "instrument_app",
            "cwd": "${workspaceFolder}/..",
            "console": "integratedTerminal"
        }
    ]
}